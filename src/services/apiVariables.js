import { generateQuery } from './helperFunctions';

export const authApi = {
  login: {
    api: 'user/login_lender/',
    method: 'post',
    baseURL: 'normal',
  },
};

export const commonApi = {};
